<div class="page-header">
    <h1><%= @user.full_name %></h1>
</div>

<% if @statuses %>
 <% @statuses.each do |status| %>
<div class="status well">
 <p><%= status.content %></p>
 <em><%= link_to time_ago_in_words(status.created_at), status_path(status) %> ago</em>
</div>
 <% end %>
<% end %>